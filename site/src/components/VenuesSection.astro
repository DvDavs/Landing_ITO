---
// Componente: Sección de sedes y municipios
import venues from '../data/venues.json';
---

<section id="sedes" class="venues-section">
  <div class="container">
    <div class="section-header">
      <h2 class="text-center">Sedes y Municipios Participantes</h2>
      <p class="text-center subtitle">Conoce los Municipios que Albergarán el Evento</p>
      
      <div class="info-box">
        <p>
          El XXXI Encuentro Nacional se desarrollará en diversos municipios de los Valles Centrales de Oaxaca, 
          cada uno con su propia riqueza cultural, histórica y tradicional. Los invitamos a conocer estos 
          maravillosos lugares durante su estancia.
        </p>
      </div>
    </div>

    <div class="venues-grid">
      {venues.map((venue) => (
        <div class="venue-card">
          <div class="venue-header">
            <h3>{venue.name}</h3>
            <span class="venue-type-badge">{venue.type}</span>
          </div>

          <div class="venue-content">
            <div class="venue-president">
              <strong>Presidente Municipal:</strong>
              <p>{venue.president}</p>
            </div>

            {venue.heritage && (
              <div class="heritage-badge">
                <span class="icon">🏛️</span> {venue.heritage}
              </div>
            )}

            <p class="venue-description">{venue.description}</p>

            {venue.highlights && venue.highlights.length > 0 && (
              <div class="highlights">
                <strong>✨ Atractivos principales:</strong>
                <ul>
                  {venue.highlights.map((highlight) => (
                    <li>{highlight}</li>
                  ))}
                </ul>
              </div>
            )}

            {venue.culturalNote && (
              <div class="cultural-note">
                <div class="cultural-note-icon">
                  <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="12" y1="16" x2="12" y2="12"></line>
                    <line x1="12" y1="8" x2="12.01" y2="8"></line>
                  </svg>
                </div>
                <div class="cultural-note-content">
                  <strong>Sabías que...</strong>
                  <p>{venue.culturalNote}</p>
                </div>
              </div>
            )}
          </div>

          <div class="venue-footer">
            <button class="btn btn-outline btn-small" onclick={`alert('Próximamente: Mapa interactivo de ${venue.name}')`}>
              📍 Ver en mapa
            </button>
          </div>
        </div>
      ))}
    </div>

    <div class="map-placeholder">
      <div class="map-content">
        <h3>Mapa de Ubicaciones</h3>
        <div class="map-box">
          <p class="map-text">
            🗺️ Próximamente: Mapa interactivo con la ubicación de todos los municipios participantes 
            y sus principales atractivos turísticos.
          </p>
          <p class="map-note">
            Todas las sedes están ubicadas en los Valles Centrales de Oaxaca, con fácil acceso 
            desde la ciudad de Oaxaca de Juárez.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .venues-section {
    padding: 4rem 0;
    background: linear-gradient(to bottom, #ffffff, #f8f8f8);
  }

  .section-header {
    margin-bottom: 3rem;
  }

  .subtitle {
    font-size: 1.25rem;
    color: var(--color-accent);
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .venues-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .venue-card {
    background: white;
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border-top: 4px solid var(--color-accent);
  }

  .venue-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }

  .venue-header {
    background: linear-gradient(135deg, rgba(93, 133, 166, 0.08), rgba(27, 57, 106, 0.08));
    padding: 1.5rem;
    border-bottom: 2px solid var(--color-border);
  }

  .venue-header h3 {
    color: var(--color-primary);
    margin-bottom: 0.5rem;
    font-size: 1.35rem;
  }

  .venue-type-badge {
    display: inline-block;
    background: var(--color-accent);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .venue-content {
    padding: 1.5rem;
  }

  .venue-president {
    margin-bottom: 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--color-border);
  }

  .venue-president strong {
    display: block;
    color: var(--color-primary);
    font-size: 0.9rem;
    margin-bottom: 0.25rem;
  }

  .venue-president p {
    color: var(--color-text);
    font-weight: 600;
    font-size: 1rem;
  }

  .heritage-badge {
    background: linear-gradient(135deg, #fff7ed, #fef3c7);
    border-left: 3px solid var(--color-secondary);
    padding: 0.75rem;
    margin-bottom: 1rem;
    border-radius: 6px;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-text);
  }

  .heritage-badge .icon {
    font-size: 1.25rem;
    margin-right: 0.5rem;
  }

  .venue-description {
    line-height: 1.7;
    margin-bottom: 1.25rem;
    color: var(--color-text-light);
    text-align: justify;
  }

  .highlights {
    margin-bottom: 1.25rem;
  }

  .highlights strong {
    display: block;
    color: var(--color-primary);
    margin-bottom: 0.75rem;
    font-size: 0.95rem;
  }

  .highlights ul {
    list-style: none;
    padding-left: 0;
  }

  .highlights li {
    padding: 0.35rem 0 0.35rem 1.5rem;
    position: relative;
    color: var(--color-text-light);
    font-size: 0.9rem;
  }

  .highlights li::before {
    content: "•";
    position: absolute;
    left: 0.5rem;
    color: var(--color-accent);
    font-weight: bold;
    font-size: 1.2rem;
  }

  .cultural-note {
    display: flex;
    gap: var(--spacing-sm);
    padding: var(--spacing-md);
    margin-top: var(--spacing-md);
    border-top: 2px solid var(--color-border);
    border-bottom: 2px solid var(--color-border);
    background: transparent;
  }

  .cultural-note-icon {
    flex-shrink: 0;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-accent) 0%, #5D85A6 100%);
    border-radius: var(--radius-md);
    color: white;
  }

  .cultural-note-icon svg {
    width: 20px;
    height: 20px;
  }

  .cultural-note-content {
    flex: 1;
  }

  .cultural-note-content strong {
    display: block;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .cultural-note-content p {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--color-text-light);
    margin: 0;
  }

  .venue-footer {
    padding: 1rem 1.5rem;
    background: var(--color-bg-alt);
    border-top: 1px solid var(--color-border);
  }

  .btn-small {
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
    width: 100%;
  }

  /* Mapa placeholder */
  .map-placeholder {
    margin-top: 4rem;
  }

  .map-content h3 {
    text-align: center;
    color: var(--color-primary);
    margin-bottom: 1.5rem;
  }

  .map-box {
    background: white;
    border: 3px dashed var(--color-border);
    border-radius: 12px;
    padding: 3rem;
    text-align: center;
    min-height: 300px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .map-text {
    font-size: 1.125rem;
    color: var(--color-text-light);
    margin-bottom: 1rem;
    max-width: 600px;
  }

  .map-note {
    font-size: 0.95rem;
    color: var(--color-accent);
    font-style: italic;
    max-width: 500px;
  }

  @media (max-width: 768px) {
    .venues-section {
      padding: 2rem 0;
    }

    .venues-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .map-box {
      padding: 2rem 1rem;
      min-height: 250px;
    }
  }
</style>
